let texto = "Hola mundo"

let textoReversado = texto.split("").reverse()

console.log(textoReversado);


// Es un metodo que no es aplicable a cadena de texto sino unicamente a arrays
// En este caso que usamos una cadena de texto primeramente la convertimos a un array de varios carcteres y luego los invertimos